apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: video-pipeline
  labels:
    app: inference-service
    app.kubernetes.io/name: inference-service
    app.kubernetes.io/component: inference
    app.kubernetes.io/part-of: video-pipeline
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app: inference-service

